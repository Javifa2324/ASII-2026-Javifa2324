# Declaración de uso de Inteligencia Artificial

Utilicé ChatGPT de OpenAI como herramienta de apoyo durante esta actividad.

La herramienta fue utilizada para revisar la aplicación del patrón Repository,
analizar la separación entre las capas Presentation, Application, Domain y
Persistence, y proponer una implementación InMemory compatible con la interfaz
BedRepository existente.

Revisé personalmente el código y verifiqué que tanto PdoBedRepository como
InMemoryBedRepository implementaran el mismo contrato.

También ejecuté las pruebas y la validación de sintaxis PHP antes de preparar
la entrega.

Comprendo la función del patrón Repository y puedo explicar por qué el servicio
depende de una interfaz y no directamente de PDO o de una base de datos.
